## 1. OpenSpec

- [x] 1.1 Add OpenSpec artifacts for alias preservation in schema projections.

## 2. Implementation

- [x] 2.1 Fix `JsonSchema` clone/assign so alias metadata is copied from the source schema.

## 3. Validation

- [x] 3.1 Add regression tests for `.omit()` preserving aliases on a Ts.ED model schema.
- [x] 3.2 Add regression tests for `.partial()` preserving aliases on a Ts.ED model schema and run the `@tsed/schema` tests.
